fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-dotenv>=1.0.1
pydantic>=2.0.0

# Database
supabase>=2.4.0

# Auth
PyJWT>=2.8.0
cryptography>=42.0.0
passlib[bcrypt]>=1.7.4

# HTTP client
httpx>=0.27.0

# AI providers
anthropic>=0.34.0
openai>=1.40.0        # OpenAI-compatible SDK used for K2 + OpenRouter

# Email
resend>=2.0.0

# SSE streaming
sse-starlette>=2.1.0

# File uploads (multipart/form-data support for FastAPI)
python-multipart>=0.0.9

# PDF extraction
pypdf>=6.8.0

# RAG / embeddings (pgvector replaces ChromaDB)
sentence-transformers>=3.0.0
numpy>=1.26.0

# Calendar (iCal parsing)
icalendar>=6.0.0
python-dateutil>=2.9.0
